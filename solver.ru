class Solver
    def factorial(n)
        return n
    end

    def reverse(word)
        word.reverse()
    end

    def fizzBuzz(n)
    when n / 3
        'fizz'
    when n / 5
        'buzz'
    when n / 3 && n / 5
        'fizzbuzz'
    end
end